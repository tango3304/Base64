from setuptools import setup, find_packages

setup(
    name='base64',
    version='0.1.0',
    packages=find_packages(),
    description='bace64 encode and decode',
        install_requires=[],
)